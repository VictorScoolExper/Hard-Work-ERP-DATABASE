import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const rootElement = document.getElementById('general-dashboard-page');

ReactDOM.createRoot(rootElement).render(<App />);