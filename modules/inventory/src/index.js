import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const rootElement = document.getElementById('inventory-page');

ReactDOM.createRoot(rootElement).render(<App />);